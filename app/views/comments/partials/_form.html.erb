<script type="text/javascript">
var simplemde = new SimpleMDE({
 element: document.getElementById("comment_text"), toolbar: false});

</script>

<% if current_user %>
<div class="well">
                   tell people what you think.  <span class="pull-right"> we support markdown </span>
                   
<%= form_for [@note, @note.comments.build] , remote: true  do |f| %>
                        <div class="form-group">
                        	<%= f.text_area :comment , { :class =>'form-control' , :rows => '4' , :id => "comment_text"} %>

                           
                        </div>
                        <%= f.submit "Submit" , { :class => 'btn btn-primary pull-right' }  %> <br/>
				<% end %>
                </div>

<% end %>

