$("#<%= params[:id] %>").empty();
$("#<%= params[:id] %>").html('<%= button_to "follow" ,follow_user_feed_path(User.find(params[:id]))  , {class: "btn btn-primary" , :method => :put , remote: true } %>');
$('#following_count').html("<%= Relationship.where(:follower_id => current_user.id).count - 1 %>");